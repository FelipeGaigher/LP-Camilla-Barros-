node_modules
dist
dist-ssr
.vercel
.env*
.DS_Store
*.log

# tarballs de transferencia
*.tar.gz

# Imagens-fonte de trabalho (originais do Gemini, recortes). O site serve
# pelo Vercel Blob, entao isto e material de bastidor: 16 MB que ficariam para
# sempre no historico de um repositorio publico sem ninguem consumir.
docs/images/
