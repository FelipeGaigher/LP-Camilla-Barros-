/**
 * Estagios do funil no client.
 *
 * Re-export, no mesmo espirito de lib/brt.js: as colunas do kanban, o CHECK do
 * banco e a validacao da API saem todos da mesma lista. Estagio e o tipo de
 * coisa que alguem acrescenta num lado e esquece no outro, e ai o card some da
 * tela sem erro nenhum aparecer.
 */
export * from '../../../api/_lib/funil.js'
