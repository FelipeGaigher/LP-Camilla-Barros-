# Banco de dados PostgreSQL (no proprio servidor Hetzner)
DATABASE_URL=postgres://camilla:SENHA_FORTE@localhost:5432/camilla_odonto

# Porta da API/servidor
PORT=3001

# Admin inicial (usado apenas no seed; troque a senha pelo painel depois)
ADMIN_USER=camilla
ADMIN_PASSWORD=troque-esta-senha

# Ambiente
NODE_ENV=production
